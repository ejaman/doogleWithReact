import React from "react";
import styles from "./footer.module.css";

const Footer = (props) => (
  <section className={styles.footer}>
    <div className={styles.info}>South Korea</div>
    <div className={styles.info}>About</div>
  </section>
);

export default Footer;
